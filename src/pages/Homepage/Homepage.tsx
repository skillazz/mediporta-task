import { Section } from "../../components/atoms/Section/Section";
import styles from "./Homepage.module.scss";

export const Homepage = () => {
	return <Section className={styles.wrapper}>hello</Section>;
};
